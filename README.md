# RipleyWorks Digital

Single-file marketing site for ripleyworksdigital.com, served with GitHub Pages.

- `index.html` is the whole site (CSS, icons, and scripts inline; Google Fonts is the only external request).
- `CNAME` binds the custom domain.
